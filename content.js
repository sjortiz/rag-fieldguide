window.COURSE = [{"id": 1, "title": "What RAG adds, and where it can fail", "html": "<p><strong>Goal:</strong> distinguish finding evidence from writing an answer.</p>\n<p>Retrieval-augmented generation supplies external material to a model while it answers. A useful first implementation is: retrieve chunks, place them alongside the question, and ask for a source-supported response. The research methods later in this course change how much retrieval happens and how evidence is selected. <a href=\"https://arxiv.org/html/2403.14403v2\" target=\"_blank\" rel=\"noopener noreferrer\">Adaptive-RAG, background and method \u2197</a>.</p>\n<p>For Harbor, imagine the system answers Q3 using only C1. It can correctly say the dock was unavailable, but cannot establish the equipment problem. If it invents a hydraulic leak without seeing C3, a fact that happens to be correct still lacks a retrieved basis. If it sees C1\u2013C3 but says the truck was late, retrieval succeeded and answer generation failed.</p>\n<p>Separate four questions when debugging:</p>\n<ol>\n<li>Is the needed information in the accessible corpus?</li>\n<li>Did retrieval find it?</li>\n<li>Did context selection retain it?</li>\n<li>Did the answer represent it accurately?</li>\n</ol>\n<p><strong>Exercise:</strong> write a one-sentence answer to Q3 with only C1, then with C1\u2013C3. Underline each claim and assign a supporting chunk. Do the same for Q5. Save this as your first evidence map.</p>\n<p><strong>Checkpoint 1:</strong> can a plausible, factually correct answer still fail an evidence-grounding requirement?</p>\n<p><strong>Optional build:</strong> represent each chunk as a record with <code>chunk_id</code>, <code>document_id</code>, <code>text</code>, <code>case_id</code>, <code>source_date</code>, and <code>access_group</code>. Keep these fields through every retrieval step.</p>"}, {"id": 2, "title": "BM25: why matching words still matters", "html": "<p><strong>Goal:</strong> explain rarity, frequency saturation, and length normalization.</p>\n<p>BM25 scores lexical matches. A term contributes according to how informative it is across the collection, how often it occurs in a document, and document length. Repetition has diminishing returns; long documents do not automatically win by containing more words. The parameter <code>k1</code> controls frequency saturation, and <code>b</code> controls length normalization. BM25 produces a ranking score, not a calibrated probability that an answer is true. <a href=\"https://www.staff.city.ac.uk/~sbrp622/papers/foundations_bm25_review.pdf\" target=\"_blank\" rel=\"noopener noreferrer\">Robertson &amp; Zaragoza, author-hosted paper \u2197</a>.</p>\n<p>A common teaching form, summing over distinct query terms, is:</p>\n<pre><code>score(q,d) = \u03a3 IDF(t) \u00d7 f(t,d)(k1 + 1)\n                        \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n                        f(t,d) + k1(1 \u2212 b + b|d|/avgdl)</code></pre>\n<p>Here, <code>f</code> is term frequency, <code>|d|</code> is document length, and <code>avgdl</code> is the collection&#x27;s average document length. Implementations vary in IDF and other details.</p>\n<p><strong>Read:</strong> the introduction, \u00a72.3, and \u00a7\u00a73.4\u20133.5. Return to BM25F in \u00a73.6 later if you need separate title/body weighting.</p>\n<p><strong>Worked exercise:</strong> isolate one term and set its IDF to 2, <code>k1=1.2</code>, and <code>b=0.75</code>. Document A has length equal to the average and term frequency 1. Document B has the same length and frequency 4. Their contributions are 2.000 and approximately 3.385. Four occurrences do not earn four times the score.</p>\n<p>Now calculate document C: frequency 1, length twice the average. What changes when <code>b=0</code>? These numbers are a mathematical illustration, not scores measured from the Harbor corpus.</p>\n<p><strong>Harbor application:</strong> list three literal strings that could be useful search anchors: a dock identifier, an equipment identifier, and a date. Compare them with Q2&#x27;s paraphrase.</p>\n<p><strong>Checkpoint 2:</strong> what is C&#x27;s score, and what happens to its length penalty when <code>b=0</code>?</p>"}, {"id": 3, "title": "Vector retrieval: finding similar meaning", "html": "<p><strong>Goal:</strong> separate the embedding model from the algorithm that searches its vectors.</p>\n<p>An embedding represents text as numbers. A query is embedded in a compatible space, and search finds nearby stored vectors. Redis supports distance measures including cosine, L2, and inner product. FLAT performs exhaustive vector comparison; HNSW uses an approximate graph-based search with tunable accuracy, speed, and memory tradeoffs. Increasing HNSW search effort can improve neighbor recall at additional query cost. Matching vector dimensions alone does not establish embedding compatibility. <a href=\"https://redis.io/docs/latest/develop/ai/search-and-query/vectors/\" target=\"_blank\" rel=\"noopener noreferrer\">Redis vector concepts \u2197</a>.</p>\n<p>For this course, begin with FLAT as a reference. Try HNSW after you can measure the difference. A larger index is a reason to benchmark alternatives; it is not a universal threshold that dictates the right algorithm.</p>\n<p><strong>Exercise:</strong> predict whether lexical or vector retrieval will better handle Q1 and Q2. Record this as a hypothesis, not a result. A model might connect \u201cchallenge a bill\u201d with \u201cinvoice dispute,\u201d but it might also retrieve a topically similar policy that lacks the deadline.</p>\n<p>Suppose a FLAT search returns IDs <code>{A,B,C,D,E}</code> and HNSW returns <code>{A,B,D,F,G}</code> for the same top-five query. Neighbor recall is <code>3/5 = 60%</code>. This measures agreement with exact vector neighbors. It does not tell you whether any of those neighbors answer the question.</p>\n<p><strong>Checkpoint 3:</strong> could perfect nearest-neighbor retrieval still deliver irrelevant evidence?</p>\n<p><strong>Optional build:</strong> embed C1\u2013C7 using one model and its documented query/document settings. Compare top-five FLAT results with HNSW. Log the embedding model, dimensions, distance metric, and HNSW settings. Do not treat hand-invented numeric vectors as semantic embeddings.</p>"}, {"id": 4, "title": "RRF: combining rankings", "html": "<p><strong>Goal:</strong> combine lexical and semantic retrieval without adding incompatible raw scores.</p>\n<p>Reciprocal Rank Fusion adds contributions based on each candidate&#x27;s position in each list:</p>\n<pre><code>RRF(d) = \u03a3 1 / (c + rank_i(d))</code></pre>\n<p>Use one-based ranks. In a truncated-list implementation, a missing candidate contributes zero for that list. The paper uses <code>c=60</code>; this constant is different from the number of results you retrieve. RRF rewards strong placement across lists, while discarding raw score magnitudes. The paper&#x27;s positive experimental results are not a guarantee for a new corpus. <a href=\"https://cormack.uwaterloo.ca/cormacksigir09-rrf.pdf\" target=\"_blank\" rel=\"noopener noreferrer\">Cormack, Clarke &amp; B\u00fcttcher, RRF paper \u2197</a>.</p>\n<p><strong>Worked exercise:</strong> these rankings are invented for arithmetic practice, not measured Harbor results.</p>\n<div class=\"table-wrap\"><table><thead><tr><th>Candidate</th><th>BM25 rank</th><th>Vector rank</th><th>RRF with c=60</th></tr></thead><tbody><tr><td>A</td><td>1</td><td>3</td><td>0.032266</td></tr><tr><td>B</td><td>2</td><td>1</td><td>0.032522</td></tr><tr><td>C</td><td>3</td><td>absent</td><td>0.015873</td></tr><tr><td>D</td><td>absent</td><td>2</td><td>0.016129</td></tr></tbody></table></div>\n<p>The combined order is <strong>B, A, D, C</strong>.</p>\n<p><strong>Exercise:</strong> recompute with <code>c=1</code>. Then remove A from the vector list and recompute with <code>c=60</code>. Explain what changed. Next, imagine both searches omit the only answer-bearing chunk: what can fusion recover?</p>\n<p><strong>Optional build:</strong> use this small implementation. Repeated IDs within one list count only once; exact fused ties break by ID for reproducibility.</p>\n<pre><code>def rrf(rankings, constant=60):\n    if constant &lt; 0:\n        raise ValueError(&quot;constant must be nonnegative&quot;)\n    scores = {}\n    for ranking in rankings:\n        unique_ids = list(dict.fromkeys(ranking))\n        for rank, chunk_id in enumerate(unique_ids, start=1):\n            scores[chunk_id] = scores.get(chunk_id, 0.0) + 1 / (constant + rank)\n    return sorted(scores.items(), key=lambda item: (-item[1], item[0]))\n\nprint(rrf([[&quot;A&quot;, &quot;B&quot;, &quot;C&quot;], [&quot;B&quot;, &quot;D&quot;, &quot;A&quot;]]))</code></pre>\n<p><strong>Checkpoint 4:</strong> why is an RRF score of 0.0325 not \u201c3.25% confidence\u201d?</p>"}, {"id": 5, "title": "Redis: what \u201cnative hybrid search\u201d means", "html": "<p><strong>Goal:</strong> map the retrieval design to Redis capabilities.</p>\n<p>Redis Search indexes hash or JSON documents and supports full-text and vector search within the same search engine. <a href=\"https://redis.io/docs/latest/develop/ai/search-and-query/\" target=\"_blank\" rel=\"noopener noreferrer\">Redis Search overview \u2197</a>.</p>\n<p>For Harbor, use this conceptual schema:</p>\n<div class=\"table-wrap\"><table><thead><tr><th>Field</th><th>Suggested type</th><th>Purpose</th></tr></thead><tbody><tr><td><code>case_id</code></td><td>TAG</td><td>Select Harbor&#x27;s records</td></tr><tr><td><code>access_group</code></td><td>TAG</td><td>Apply an application-enforced access scope</td></tr><tr><td><code>text</code></td><td>TEXT</td><td>Search words in the chunk</td></tr><tr><td><code>embedding</code></td><td>VECTOR</td><td>Search embedded meaning</td></tr><tr><td><code>source_time</code></td><td>NUMERIC</td><td>Restrict by an encoded timestamp when needed</td></tr><tr><td><code>chunk_id</code>, source reference</td><td>Stored metadata</td><td>Recover and cite the original passage</td></tr></tbody></table></div>\n<p>TAG fields match categorical values rather than applying normal full-text token analysis. A TAG field provides filtering machinery; your application must supply the correct permitted scope. <a href=\"https://redis.io/docs/latest/develop/ai/search-and-query/advanced-concepts/tags/\" target=\"_blank\" rel=\"noopener noreferrer\">Redis TAG documentation \u2197</a>.</p>\n<p>Distinguish three operations:</p>\n<div class=\"table-wrap\"><table><thead><tr><th>Operation</th><th>What happens</th></tr></thead><tbody><tr><td>Filtered vector search</td><td>Find nearest vectors within an eligible subset</td></tr><tr><td>Application-side fusion</td><td>Run lexical and vector searches, then merge their rankings in your code</td></tr><tr><td>Server-side fusion</td><td>Ask Redis to run and combine both branches</td></tr></tbody></table></div>\n<p>A keyword condition inside a filtered KNN query constrains eligibility; it does not automatically create two rankings and fuse them. <a href=\"https://redis.io/docs/latest/develop/ai/search-and-query/query/vector-search/\" target=\"_blank\" rel=\"noopener noreferrer\">Redis vector queries \u2197</a>.</p>\n<p>Current Redis documentation lists <code>FT.HYBRID</code> from Redis Open Source 8.4.0, with RRF and linear combination options. RRF is the documented default. Server and client compatibility still matter. For an older deployment without this command, separate supported queries plus application-side RRF provide a straightforward teaching path. <a href=\"https://redis.io/docs/latest/commands/ft.hybrid/\" target=\"_blank\" rel=\"noopener noreferrer\">FT.HYBRID reference \u2197</a>.</p>\n<p>Current scoring documentation names <code>BM25STD</code> and notes the older <code>BM25</code> name&#x27;s deprecation. Record the actual scorer and server version when comparing experiments. <a href=\"https://redis.io/docs/latest/develop/ai/search-and-query/advanced-concepts/scoring/\" target=\"_blank\" rel=\"noopener noreferrer\">Redis scoring \u2197</a>.</p>\n<p><strong>Exercise:</strong> draw the two retrieval branches for Q3. Apply case and access restrictions to both. Retrieve up to 20 candidates per branch, fuse, and select up to five chunks. These are starting exercise settings; the tiny corpus will return fewer than 20. State where embeddings are generated and where the final answer is written.</p>\n<p><strong>Optional command study:</strong> after creating an appropriate index and loading real embeddings, this illustrates the shape of an unrestricted hybrid query over a permitted-only index. <code>&lt;binary-vector&gt;</code> is a placeholder for a client&#x27;s binary parameter, not a literal vector string.</p>\n<pre><code>FT.HYBRID harbor_public_idx\n  SEARCH &quot;@text:(dock | crane)&quot;\n  VSIM @embedding $q\n  KNN 2 K 20\n  COMBINE RRF 4 WINDOW 20 CONSTANT 60\n  LIMIT 0 5\n  PARAMS 2 q &lt;binary-vector&gt;</code></pre>\n<p>Use the command reference for your installed version before running it. For a shared index, enforce scope in each branch; filtering only one branch can admit out-of-scope candidates through the other.</p>\n<p><strong>Repository exercise:</strong> open <a href=\"https://github.com/RediSearch/RediSearch\" target=\"_blank\" rel=\"noopener noreferrer\">RediSearch on GitHub \u2197</a>, locate release notes for your chosen version, and record one relevant search feature. Treat repository <code>master</code> and an installed release as potentially different code.</p>\n<p><strong>Checkpoint 5:</strong> does Redis providing text search and vector KNN mean it also decides when the evidence is sufficient?</p>"}, {"id": 6, "title": "Routing: use the LLM for an unresolved choice", "html": "<p><strong>Goal:</strong> distinguish route selection, fusion, and reranking.</p>\n<p>LlamaIndex&#x27;s <code>RouterQueryEngine</code> selects among query engines, using descriptions and a selector; it supports selecting one or multiple destinations. LangChain&#x27;s current routing examples likewise classify input and direct execution. These are mechanisms for choosing a path. <a href=\"https://developers.llamaindex.ai/python/framework/module_guides/querying/router/\" target=\"_blank\" rel=\"noopener noreferrer\">LlamaIndex routers \u2197</a>, <a href=\"https://docs.langchain.com/oss/python/langgraph/workflows-agents\" target=\"_blank\" rel=\"noopener noreferrer\">LangGraph routing \u2197</a>.</p>\n<p>Your proposed \u201cLLM as tie-breaker\u201d is a design choice we will test: apply clear rules first, and ask the LLM only when the remaining route is ambiguous. If it instead compares retrieved passages to decide their order, that is reranking. A tiny gap between RRF scores is not itself evidence that route selection is ambiguous.</p>\n<p><strong>Exercise:</strong> design a router with three destinations: <code>hybrid</code>, <code>graph_local</code>, and <code>global_summary</code>. Write one description and two example questions per destination. Add a <code>clarify</code> outcome for requests such as \u201cWhat happened there?\u201d with no usable context.</p>\n<p>For an ambiguous query, have the router return a destination and a short, checkable justification tied to the question. Do not use a self-reported confidence percentage as your only acceptance rule.</p>\n<p>Try ten paraphrases of Q3 and Q4. Record whether the chosen route changes and whether it still answers correctly. Compare against always using hybrid retrieval. If routing costs more and improves nothing in this exercise, keep the simpler policy.</p>\n<p><strong>Read:</strong> the LlamaIndex usage pattern and selector descriptions, then the routing section of the linked LangGraph guide. Your original <a href=\"https://python.langchain.com/v0.2/docs/how_to/routing/\" target=\"_blank\" rel=\"noopener noreferrer\">LangChain v0.2 routing link \u2197</a> is a versioned historical reference; use current documentation for new code.</p>\n<p><strong>Checkpoint 6:</strong> an LLM chooses passage B over passage A after reading both. Is that routing or reranking?</p>"}, {"id": 7, "title": "Graph expansion: follow evidence-bearing relationships", "html": "<p><strong>Goal:</strong> recover connected facts that the original query may not name.</p>\n<p>In our proposed Stage 2, the first retrieval supplies starting chunks or entities. A graph then reveals nearby relationships, and each useful relationship leads back to source text. This is an application design exercise. Microsoft GraphRAG&#x27;s local search similarly combines graph information with source text, but its actual context construction is more elaborate than this small example. <a href=\"https://microsoft.github.io/graphrag/query/local_search/\" target=\"_blank\" rel=\"noopener noreferrer\">GraphRAG local search \u2197</a>.</p>\n<p>Build the following source-backed chain:</p>\n<pre><code>Harbor delay on May 12\n  -- occurred because --&gt; Dock 4 unavailable [C1]\n  -- unavailable because --&gt; Crane 7 failed inspection [C2]\n  -- inspection failed because --&gt; hydraulic leak [C3]</code></pre>\n<p>Store an edge&#x27;s direction, relation type, date or event scope, and source chunk. \u201cNorthstar supplied a seal\u201d must not become \u201cNorthstar caused the failure.\u201d Shared names alone do not establish the intended entity or event.</p>\n<p><strong>Exercise:</strong> add C4 and C7 to your graph. Start with C1 and follow at most two edges to answer Q3. For this exercise, the starting event is hop zero. Mark the edge that supplies each new fact. Then add 20 irrelevant documents mentioning Harbor and consider why unrestricted expansion would overwhelm your context.</p>\n<p>Choose a two-hop limit, at most six added chunks, and relations relevant to the question. These are trial budgets. Keep a visited set to avoid cycles and recheck access before including any source. Record why each added chunk matters.</p>\n<p><strong>Checkpoint 7:</strong> why can a graph path linking a supplier to a broken machine fail to prove responsibility?</p>\n<p><strong>Optional build:</strong> represent edges as a list of records and use a simple bounded traversal. You do not need a separate graph database for eight source chunks. Do not confuse HNSW&#x27;s vector-neighbor graph with this graph of named entities and relationships.</p>"}, {"id": 8, "title": "Microsoft GraphRAG: local facts and global themes", "html": "<p><strong>Goal:</strong> explain what the paper adds beyond neighboring-entity expansion.</p>\n<p>The GraphRAG paper builds an entity graph from source documents, organizes it into communities, and produces community summaries. It uses these summaries to support questions about patterns across a corpus. Its central research focus is query-focused summarization at a broader scope than retrieving a few similar passages. <a href=\"https://arxiv.org/html/2404.16130v2\" target=\"_blank\" rel=\"noopener noreferrer\">Edge et al., GraphRAG paper \u2197</a>.</p>\n<p>The implementation distinguishes local, entity-centered context gathering from global search over community reports. Global search creates intermediate responses from report batches and combines selected material into a final response. It can enter through community reports without first following our BM25 \u2192 RRF \u2192 graph expansion sequence. <a href=\"https://microsoft.github.io/graphrag/query/local_search/\" target=\"_blank\" rel=\"noopener noreferrer\">Local search \u2197</a>, <a href=\"https://microsoft.github.io/graphrag/query/global_search/\" target=\"_blank\" rel=\"noopener noreferrer\">global search \u2197</a>.</p>\n<p><strong>Exercise:</strong> make two hand-written reports: \u201cequipment and dock disruption\u201d from C1\u2013C3 and C7, and \u201ctransport delay\u201d from C5. Cite the underlying chunks. Use both reports to answer Q4 in 80 words. Then answer Q1 directly from C7. Compare the amount of preparation and context required.</p>\n<p>Deliberately remove the transport report and answer Q4 again. Identify the missing theme. This demonstrates a possible coverage failure; the tiny corpus cannot establish a general performance advantage for GraphRAG.</p>\n<p><strong>Read:</strong> the paper&#x27;s pipeline and global-answer method, then its evaluation question and limitations. Ask what was measured and whether your question types resemble the study&#x27;s. Explore the <a href=\"https://github.com/microsoft/graphrag\" target=\"_blank\" rel=\"noopener noreferrer\">reference implementation \u2197</a> and <a href=\"https://microsoft.github.io/graphrag/\" target=\"_blank\" rel=\"noopener noreferrer\">documentation \u2197</a>. Your supplied <a href=\"https://aka.ms/graphrag\" target=\"_blank\" rel=\"noopener noreferrer\">aka.ms link \u2197</a> is also retained here for reference.</p>\n<p><strong>Checkpoint 8:</strong> which is the better initial fit: local evidence for Q1 or a global community-summary answer? Why?</p>"}, {"id": 9, "title": "Self-RAG: retrieve, generate, and critique", "html": "<p><strong>Goal:</strong> separate the research method from a simple prompted review loop.</p>\n<p>Self-RAG trains a model to emit reflection tokens alongside generated text. These signals concern whether retrieval is useful, whether a passage is relevant, whether a generated segment is supported, and how useful the response is. Training and inference both contribute to the method. Asking an ordinary LLM to \u201ccheck your answer\u201d does not reproduce the trained Self-RAG system. <a href=\"https://arxiv.org/html/2310.11511v1\" target=\"_blank\" rel=\"noopener noreferrer\">Asai et al., Self-RAG \u2197</a>.</p>\n<p><strong>Read:</strong> Figure 1, the reflection-token table, and \u00a73&#x27;s training/inference overview. You should be able to identify which behavior was learned and which behavior is imposed by decoding or application logic.</p>\n<p><strong>Exercise:</strong> audit this deliberately faulty answer to Q5:</p>\n<blockquote>Northstar caused the May 12 delay by supplying a defective seal. Dock 4 reopened on May 14.</blockquote>\n<p>For each claim, assign one of <code>supported</code>, <code>contradicted</code>, or <code>not established</code>, and give the evidence. The supplier record documents a replacement delivered on May 13. It does not establish who caused the earlier failure. The reopening date has direct support in C7.</p>\n<p>Now write a bounded review instruction: extract claims, associate each with a passage, flag missing support, and identify one concrete evidence gap. Call this a <strong>Self-RAG-inspired exercise</strong>, since it does not train reflection tokens.</p>\n<p><strong>Checkpoint 9:</strong> does \u201cI am confident\u201d from the answering model demonstrate that its claims are supported?</p>"}, {"id": 10, "title": "Adaptive-RAG: choose how much retrieval to do", "html": "<p><strong>Goal:</strong> choose an initial strategy according to the question&#x27;s needs.</p>\n<p>Adaptive-RAG trains a smaller classifier to choose among no retrieval, single-step retrieval, and multi-step retrieval. Its training labels use signals from strategy outcomes and dataset characteristics. This is chiefly an initial strategy decision, rather than a loop that starts only after a bad answer. <a href=\"https://arxiv.org/html/2403.14403v2\" target=\"_blank\" rel=\"noopener noreferrer\">Jeong et al., Adaptive-RAG \u2197</a>.</p>\n<p>For our course application, additionally require source lookup for case-specific facts even when the question is short. \u201cWhen did Dock 4 reopen?\u201d is simple in form, but its answer depends on this case file. A no-retrieval path is more suitable here for rewriting text already supplied in the question.</p>\n<p><strong>Exercise:</strong> assign a starting strategy to: rewrite a provided sentence; Q1; Q3; Q4; and Q6. Explain what evidence is needed. Do not label every long question \u201ccomplex\u201d or every short question \u201ceasy.\u201d</p>\n<p>Create a small comparison with three fixed strategies and your selected strategy. Log correctness, number of retrieval calls, and elapsed time. A useful classifier must save work or improve answers relative to a fixed baseline; route-label accuracy alone does not establish that.</p>\n<p><strong>Checkpoint 10:</strong> how does initial strategy selection differ from checking whether a draft answer has enough support?</p>"}, {"id": 11, "title": "The \u201csad path\u201d: identify the gap, then act", "html": "<p><strong>Goal:</strong> design useful recovery and explicit stopping conditions.</p>\n<p>This lesson combines ideas from the preceding papers into an original application policy. It is not an implementation claimed by either paper. \u201cNeed more\u201d should identify missing evidence or failed execution, not just repeat the same search.</p>\n<div class=\"table-wrap\"><table><thead><tr><th>Observed problem</th><th>Proposed next action</th><th>Stop condition</th></tr></thead><tbody><tr><td>Nothing relevant retrieved</td><td>Inspect filters and try an alias or paraphrase</td><td>One alternative adds no useful evidence</td></tr><tr><td>Missing link in an event chain</td><td>Retrieve the named intermediate entity/event</td><td>Required link found or graph budget reached</td></tr><tr><td>Question has several subparts</td><td>Search specifically for the unanswered part</td><td>All parts supported or remaining part unavailable</td></tr><tr><td>Sources disagree</td><td>Check dates, event scope, and original passages</td><td>Explain unresolved disagreement if it remains</td></tr><tr><td>Source is unavailable to this reader</td><td>State the accessible evidence gap</td><td>Do not widen access as a search retry</td></tr><tr><td>Search service errors</td><td>Use one bounded retry or a configured fallback</td><td>Report the failure if neither works</td></tr><tr><td>Request is ambiguous</td><td>Ask a focused clarifying question</td><td>Wait for the missing scope</td></tr></tbody></table></div>\n<p>For the Harbor exercise, allow the initial retrieval cycle plus <strong>at most two additional retrieval cycles</strong>. Count a cycle as one policy-selected evidence-gathering round, and separately log the number of actual engine calls. Set a maximum of ten retained chunks, and stop early if a retry adds no new relevant evidence. These are teaching settings to test, not production defaults.</p>\n<p><strong>Exercise:</strong> simulate Q3 with an initial result containing only C1. Record the next query, newly found evidence, and stop reason. Repeat with Q6 and explain why more fluent drafting cannot supply the missing amount.</p>\n<p>For Q5, a valid result is: \u201cThe available records show that Northstar supplied a replacement seal after the inspection failure. They do not establish that Northstar caused it.\u201d Preserve that distinction even if many retrieved passages mention the supplier.</p>\n<p><strong>Checkpoint 11:</strong> after a retry returns the same insufficient evidence, what should the system do next?</p>"}, {"id": 12, "title": "Evaluation and capstone", "html": "<p><strong>Goal:</strong> demonstrate which components improve the system and what they cost.</p>\n<p>Your capstone is a design for a Harbor research assistant. The minimum submission is an architecture and evaluation workbook written in any format you prefer. Building the running application is optional additional work.</p>\n<p><strong>Submit these six items:</strong></p>\n<ol>\n<li>An ingestion plan: chunk IDs, source links, text and vector fields, embedding configuration, and update handling.</li>\n<li>A Stage 1 plan: BM25, vector retrieval, shared scope filters, RRF, and separate candidate/context limits.</li>\n<li>A routing policy: clear rules, the ambiguous cases sent to an LLM, and a clarification outcome.</li>\n<li>A Stage 2 plan: graph relations, provenance, expansion limits, and when global community reports help.</li>\n<li>A recovery policy: evidence checks, possible next actions, resource limits, and stop behavior.</li>\n<li>An evaluation report: results by question type, at least three failures, and a justified recommendation.</li>\n</ol>\n<h3>Build a small evaluation set</h3>\n<p>Start with 30 questions: five each for exact facts, paraphrases, multi-hop chains, global summaries, unsupported premises, and missing evidence. Mark expected answers and required source IDs before running your system. Use 15 for development and keep 15 unseen for a final comparison, splitting near-duplicate question families together to reduce leakage. This is a learning experiment; 15 held-out questions are too few for strong general claims.</p>\n<p>The eight-chunk corpus is enough for a paper exercise. For a software capstone, expand it with clearly labeled fictional distractors, aliases, and dated events. Keep the same corpus and embedding model across comparisons unless that is the variable you are testing.</p>\n<h3>Measure different stages separately</h3>\n<div class=\"table-wrap\"><table><thead><tr><th>Measure</th><th>Definition for this course</th><th>What it diagnoses</th></tr></thead><tbody><tr><td>Evidence Recall@k</td><td>Required chunks retrieved / all required chunks</td><td>Whether retrieval found the necessary evidence</td></tr><tr><td>Complete evidence rate</td><td>Questions where every required chunk was found / answerable questions</td><td>Missing links in multi-hop questions</td></tr><tr><td>Reciprocal rank</td><td>1 / position of first relevant chunk; 0 if absent</td><td>How early useful evidence appears</td></tr><tr><td>Claim support rate</td><td>Supported factual claims / factual claims made</td><td>Whether the answer stays within evidence</td></tr><tr><td>Answer completeness</td><td>Required answer elements supplied / required elements</td><td>Whether the answer covers the question</td></tr><tr><td>Correct abstention rate</td><td>Correctly declined unanswerable questions / unanswerable questions</td><td>Behavior when evidence is missing</td></tr><tr><td>False abstention rate</td><td>Declined answerable questions / answerable questions</td><td>Whether the system gives up too readily</td></tr><tr><td>Cost and latency</td><td>Tokens, engine calls, median and p95 elapsed time</td><td>Operational tradeoffs</td></tr></tbody></table></div>\n<p>These are explicit course definitions so your comparisons use the same denominators. Score global answers against expected themes as well as source support. For Q3, retrieving C1 and C2 gives evidence recall <code>2/3</code>, but does not complete the equipment-cause chain. A short abstention must not earn a perfect claim-support score simply because it makes no claims; mark that case separately.</p>\n<h3>Compare components by removing or adding one</h3>\n<p>Run the same questions through BM25 only, vectors only, BM25 + vectors + RRF, hybrid + graph expansion, and the full routing/recovery design. Separately compare local retrieval and community-summary retrieval on global questions. An improvement may help one category and hurt another; show both.</p>\n<p>For each configuration, preserve the prompt, model settings, source corpus, and context budget as far as the comparison permits. Inspect actual answers rather than relying entirely on a model judging its own output. With a small sample, show individual failures alongside averages.</p>\n<p><strong>Capstone rubric:</strong> retrieval design 25 points; evidence/provenance 25; routing and recovery 20; evaluation quality 20; clarity and reproducibility 10. Aim for 80/100 and correct the following regardless of score: out-of-scope evidence entering answers, invented causal claims, and a recovery loop without a bound.</p>\n<p><strong>Checkpoint 12:</strong> if adding graph expansion improves multi-hop answers but increases latency and harms exact lookups, what change should you test?</p>"}];
